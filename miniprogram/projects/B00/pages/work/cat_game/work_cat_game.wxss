page {
  background: #f5efe4;
  min-height: 100vh;
}

/* 顶部状态栏 */
.game-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 60rpx 24rpx 20rpx;
  background: linear-gradient(135deg, #ff6b6b 0%, #ffa07a 100%);
}
.header-left { width: 80rpx; }
.back-icon { font-size: 40rpx; color: #fff; }
.header-center { flex: 1; text-align: center; }
.header-title { font-size: 34rpx; color: #fff; font-weight: 600; }
.header-right { display: flex; align-items: center; }
.resource-bar { display: flex; gap: 16rpx; }
.resource-item { display: flex; align-items: center; background: rgba(255,255,255,0.25); border-radius: 20rpx; padding: 4rpx 12rpx; }
.res-icon { font-size: 24rpx; margin-right: 4rpx; }
.res-value { font-size: 24rpx; color: #fff; font-weight: 600; }

/* 离线收益弹窗 */
.offline-modal {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 999;
}
.offline-card {
  background: #fff;
  border-radius: 24rpx;
  padding: 48rpx;
  width: 560rpx;
  text-align: center;
}
.offline-title { font-size: 36rpx; font-weight: 600; color: #333; display: block; margin-bottom: 12rpx; }
.offline-desc { font-size: 26rpx; color: #888; display: block; margin-bottom: 24rpx; }
.offline-rewards { display: flex; flex-direction: column; gap: 12rpx; margin-bottom: 32rpx; }
.offline-reward-item { font-size: 30rpx; color: #ff6b6b; }
.offline-btn {
  background: linear-gradient(135deg, #ff6b6b, #ffa07a);
  color: #fff;
  border-radius: 40rpx;
  padding: 16rpx 0;
  font-size: 30rpx;
  font-weight: 600;
}

/* 主场景 */
.studio-scene {
  margin: 24rpx;
  height: 360rpx;
  background: linear-gradient(180deg, #e8f4f8 0%, #d4ecf0 100%);
  border-radius: 24rpx;
  overflow: hidden;
  position: relative;
}
.scene-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
}
.cat-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 16rpx;
}
.cat-avatar.cloud { background: linear-gradient(135deg, #e8f4f8, #b8d8e8); }
.cat-avatar.camera { background: linear-gradient(135deg, #fff3e0, #ffe0b2); }
.cat-avatar.star { background: linear-gradient(135deg, #f3e5f5, #e1bee7); }
.cat-emoji { font-size: 60rpx; }
.cat-greeting { font-size: 30rpx; color: #4f5368; font-weight: 600; }
.cat-career { font-size: 24rpx; color: #888; margin-top: 8rpx; }

/* 猫咪状态面板 */
.status-panel {
  margin: 0 24rpx 16rpx;
  background: #fff;
  border-radius: 20rpx;
  padding: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.05);
}
.status-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}
.pet-name { font-size: 30rpx; font-weight: 600; color: #333; }
.pet-phase {
  font-size: 22rpx;
  color: #ff6b6b;
  background: #fff0f0;
  padding: 4rpx 14rpx;
  border-radius: 12rpx;
  margin-left: 12rpx;
}
.pet-level { font-size: 34rpx; font-weight: 700; color: #ff6b6b; }
.exp-bar {
  height: 28rpx;
  background: #f0f0f0;
  border-radius: 14rpx;
  overflow: hidden;
  position: relative;
}
.exp-fill {
  height: 100%;
  background: linear-gradient(90deg, #ff6b6b, #ffa07a);
  border-radius: 14rpx;
  transition: width 0.3s ease;
}
.exp-text {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  text-align: center;
  font-size: 20rpx;
  color: #666;
  line-height: 28rpx;
}

/* 签到 */
.checkin-bar {
  margin: 0 24rpx 24rpx;
  background: #fff;
  border-radius: 16rpx;
  padding: 20rpx 24rpx;
  display: flex;
  align-items: center;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.04);
}
.checkin-icon { font-size: 36rpx; margin-right: 12rpx; }
.checkin-text { font-size: 28rpx; color: #4f5368; }

/* 小游戏列表 */
.section-title {
  margin: 0 24rpx 16rpx;
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}
.game-list { margin: 0 24rpx; }
.game-card {
  background: #fff;
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 16rpx;
  display: flex;
  align-items: center;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.04);
  transition: transform 0.15s ease;
}
.game-card:active { transform: scale(0.97); }
.game-icon-wrap {
  width: 80rpx;
  height: 80rpx;
  border-radius: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 20rpx;
  flex-shrink: 0;
}
.game-icon-wrap.photo { background: linear-gradient(135deg, #ff6b6b, #ff8e8e); }
.game-icon-wrap.retouch { background: linear-gradient(135deg, #a18cd1, #c4a7e7); }
.game-icon-wrap.post { background: linear-gradient(135deg, #fbc2eb, #f6a5c1); }
.game-icon-emoji { font-size: 40rpx; }
.game-info { flex: 1; }
.game-name { font-size: 30rpx; font-weight: 600; color: #333; display: block; }
.game-desc { font-size: 24rpx; color: #999; margin-top: 6rpx; display: block; }
.game-arrow { font-size: 36rpx; color: #ccc; }

/* 底部功能 */
.bottom-actions {
  margin: 32rpx 24rpx 48rpx;
  display: flex;
  gap: 20rpx;
}
.action-btn {
  flex: 1;
  background: #fff;
  border-radius: 20rpx;
  padding: 28rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.04);
}
.action-icon { font-size: 48rpx; margin-bottom: 8rpx; }
.action-text { font-size: 26rpx; color: #4f5368; }
